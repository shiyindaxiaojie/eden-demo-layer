spring:
  cloud:
    nacos:
      config:
        username: nacos
        password: puyiwm
        server-addr: localhost:8848
        namespace: demo
        group: spring-cloud
      discovery:
        username: nacos
        password: puyiwm
        server-addr: localhost:8848
        namespace: demo
        group: spring-cloud

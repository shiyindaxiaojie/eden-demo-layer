spring:
  application:
    name: eden-demo-layer
  cloud:
    nacos:
      config:
        name: ${spring.application.name}
        file-extension: yaml
  profiles:
    active: local # 默认启用本地环境

logging: # 日志管理
  file:
    name: ${spring.application.name}
